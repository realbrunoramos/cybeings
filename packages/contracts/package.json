{
  "name": "@cybeings/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "test": "hardhat test",
    "test:coverage": "hardhat coverage",
    "clean": "hardhat clean",
    "lint": "echo \"[contracts] no solidity lint yet\" && exit 0",
    "type-check": "tsc --noEmit --project tsconfig.json 2>/dev/null || echo \"[contracts] no tsconfig yet\" && exit 0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^17.4.2",
    "hardhat": "^2.26.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}